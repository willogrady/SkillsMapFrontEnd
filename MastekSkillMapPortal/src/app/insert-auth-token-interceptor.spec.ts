import { InsertAuthTokenInterceptor } from './insert-auth-token-interceptor';

describe('InsertAuthTokenInterceptor', () => {
  it('should create an instance', () => {
    expect(new InsertAuthTokenInterceptor()).toBeTruthy();
  });
});
